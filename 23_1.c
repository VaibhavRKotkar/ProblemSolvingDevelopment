// 1. Display ASCII Table

#include<stdio.h>

void DisplayASCII()
{
    int asciValue= 0;

    printf (" The complete ASCII table of the characters in the C ");

    for (asciValue = 0; asciValue < 255; asciValue++)
    {
        printf (" \n The value of '%c' character is: %d", asciTable, asciTable);
    }
}

int main()
{
    DisplayASCII();

    return 0;
}